# <title>

## User story

As a [type_of_user], I want [goal], so that [reason_or_benefit].

<!--
**1. The story card itself (INVEST criteria)** — a well-formed story should be:

- **I**ndependent — can be developed/delivered without depending on other stories
- **N**egotiable — details aren't locked in; it's a conversation starter, not a contract
- **V**aluable — delivers clear value to a user or customer
- **E**stimable — the team can size it (story points, t-shirt size, etc.)
- **S**mall — fits in a single sprint/iteration
- **T**estable — there's a clear way to verify it's done

2. Acceptance Criteria — the specific, testable conditions that define "done." Often written in Gherkin/BDD format:

Given [initial context], When [action taken], Then [expected outcome]

Example: Given I'm on the search results page, when I set a max price filter, then only items at or below that price are shown.
3. Supporting metadata — in a standardized backlog (Jira, Azure DevOps, etc.), a fully-specified story usually also carries:

Priority / business value
Story points or estimate
Definition of Done (team-wide checklist, separate from per-story acceptance criteria)
Dependencies / linked stories or epics
Non-functional requirements if relevant (performance, security, accessibility)
-->

## Acceptance criteria


Given [initial context], When [action taken], Then [expected outcome]
Given [initial context], When [action taken], Then [expected outcome]
Given [initial context], When [action taken], Then [expected outcome]
Given [initial context], When [action taken], Then [expected outcome]
Given [initial context], When [action taken], Then [expected outcome]

## Definition of Done

[DONE_definition]
